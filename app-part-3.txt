<span>${r.carbs??'—'} g carbs</span><span>${r.fat??'—'} g fat</span></div><div class="recipe-actions"><button class="button secondary" data-detail="${r.code}">View recipe</button><button class="button primary" data-use="${r.code}">Use today</button></div></article>`).join('');$$('[data-detail]').forEach(b=>b.onclick=()=>openRecipe(b.dataset.detail));$$('[data-use]').forEach(b=>b.onclick=()=>useRecipe(b.dataset.use));$$('[data-fav]').forEach(b=>b.onclick=()=>toggleFav(b.dataset.fav));}
function openRecipe(c){const r=state.recipes.find(x=>x.code===c);$('#recipe-detail').innerHTML=`<h2>${r.code} ${r.name}</h2><p>${r.category} • ${r.calories} kcal • ${r.protein} g protein • ${r.time}</p><div class="recipe-dialog-section"><h3>Exact ingredients</h3><ul>${r.ingredients.map(x=>`<li>${x}</li>`).join('')}</ul></div><div class="recipe-dialog-section"><h3>Method</h3><ol>${r.method.map(x=>`<li>${x.replace(/^\d+\.\s*/, '')}</li>`).join('')}</ol></div><p><strong>Make it taste excellent:</strong> ${r.taste}</p><p><strong>Batch / storage:</strong> ${r.batchCooking} ${r.storage}</p><p><strong>Work packing:</strong> ${r.workPacking}</p><p><strong>Cronometer:</strong> ${r.tracking}</p><p><strong>Pairing:</strong> ${r.pairing}</p><p class="authority-note">${AUTHORITY_NOTE}</p>`;$('#recipe-dialog').showModal();}
async function toggleFav(c){const r=state.recipes.find(x=>x.code===c);r.favourite=!r.favourite;await db.put('recipes',r);renderRecipes()}
function useRecipe(c){const r=state.recipes.find(x=>x.code===c),id={Breakfast:'plan-b',Lunch:'plan-l',Snack:'plan-s','Late top-up':'plan-t'}[r.category];$('#'+id).value=c;planUpdate();}
async function savePlan(){const d=$('#meal-date').value,l=getLog(d)||{date:d};Object.assign(l,{breakfastCode:$('#plan-b').value,lunchCode:$('#plan-l').value,snackCode:$('#plan-s').value,topUpCode:$('#plan-t').value});await db.put('dailyLogs',l);upsert(state.logs,l,'date');toast('Meal plan saved.');renderToday();}
function renderWeekly(){if(!$('#week-date').value)$('#week-date').value=sunday(today());const s=weeklySummary($('#week-date').value),prev=weeklySummary(addDays(s.start,-7)),change=Number.isFinite(s.avgWeight)&&Number.isFinite(prev.avgWeight)?prev.avgWeight-s.avgWeight:undefined;$('#weekly-kpis').innerHTML=[kpi('Week',`${s.start.slice(5)} to ${s.end.slice(5)}`,`${s.readings} valid weights`),kpi('Average weight',Number.isFinite(s.avgWeight)?`${s.avgWeight.toFixed(2)} kg`:'No data',Number.isFinite(change)?`${change.toFixed(2)} kg lost vs previous week`:'No comparison'),kpi('Average calories',Number.isFinite(s.avgCalories)?s.avgCalories.toFixed(0):'No data',`${s.daysCalories} days in range`),kpi('Average protein',Number.isFinite(s.avgProtein)?`${s.avgProtein.toFixed(0)} g`:'No data',`${s.daysProtein} days ≥110 g`),kpi('Walking',`${Math.round((s.walkAd||0)*100)}%`,`${s.walkDays}/7 completed`),kpi('Logging',`${Math.round((s.logAd||0)*100)}%`),kpi('Sleep',Number.isFinite(s.avgSleep)?`${s.avgSleep.toFixed(1)} h`:'No data'),kpi('Combined adherence',Number.isFinite(s.combined)?`${Math.round(s.combined*100)}%`:'No data','Workouts excluded')].join('');const saved=state.weekly.find(x=>x.weekStart===s.start)||{};setForm($('#weekly-form'),saved);$('#weekly-form').dataset.start=s.start;}
async function saveWeekly(e){e.preventDefault();const o=readForm(e.target);o.weekStart=e.target.dataset.start;o.weekEnd=addDays(o.weekStart,6);Object.assign(o,weeklySummary(o.weekEnd));await db.put('weeklyCheckins',o);upsert(state.weekly,o,'weekStart');toast('Weekly check-in saved.');}
function renderMeasurements(){const list=[...state.measurements].sort((a,b)=>b.date.localeCompare(a.date));$('#measurement-history').innerHTML=list.length?list.map(x=>`<article class="history-item"><div class="history-item-header"><strong>${fmt(x.date)}</strong><button class="text-button" data-del-measure="${x.id}">Delete</button></div><p>Waist ${x.waistCm??'—'} • Chest ${x.chestCm??'—'} • Hips ${x.hipsCm??'—'} • Arm ${x.rightArmCm??'—'} • Thigh ${x.rightThighCm??'—'} cm</p><p class="hint">${x.notes||''}</p></article>`).join(''):'<p class="notice neutral">No measurements yet.</p>';$$('[data-del-measure]').forEach(b=>b.onclick=async()=>{if(confirm('Delete this measurement?')){await db.delete('measurements',b.dataset.delMeasure);state.measurements=state.measurements.filter(x=>x.id!==b.dataset.delMeasure);renderMeasurements()}})}
async function saveMeasurement(e){e.preventDefault();const o=readForm(e.target);o.id=o.date;await db.put('measurements',o);upsert(state.measurements,o,'id');toast('Measurement saved.');renderMeasurements()}
function renderAdjustments(){const list=[...state.adjustments].sort((a,b)=>b.date.localeCompare(a.date));$('#adjustment-history').innerHTML=list.length?list.map(x=>`<article class="history-item"><div class="history-item-header"><div><strong>${fmt(x.date)} — ${x.adjustmentType}</strong><p>${x.previousValue||'—'} → ${x.newValue||'—'}</p></div><button class="text-button" data-del-adjust="${x.id}">Delete</button></div><p>${x.reason||''}</p><p class="hint">Evidence: ${x.evidencePeriod||'—'} • Review: ${x.reviewDate||'—'}</p></article>`).join(''):'<p class="notice neutral">No adjustments recorded.</p>';$$('[data-del-adjust]').forEach(b=>b.onclick=async()=>{if(confirm('Delete this adjustment?')){await db.delete('adjustments',b.dataset.delAdjust);state.adjustments=state.adjustments.filter(x=>x.id!==b.dataset.delAdjust);renderAdjustments()}})}
async function saveAdjustment(e){e.preventDefault();const o=readForm(e.target);o.id=`${o.date}-${crypto.randomUUID()}`;await db.put('adjustments',o);state.adjustments.push(o);e.target.reset();toast('Adjustment recorded.');renderAdjustments()}
function renderSettings(){$('#fixed-settings').innerHTML=`<div class="form-grid">${[['Name',state.profile.name],['Timezone',state.profile.timezone],['Height',`${state.profile.heightCm} cm`],['Start',`${state.profile.startWeightKg} kg on ${state.profile.startDate}`],['Goal',`${state.profile.goalWeightKg} kg by ${state.profile.deadline}`],['Calories',`${state.profile.calorieMin}–${state.profile.calorieMax}`],['Protein',`${state.profile.proteinMin}–${state.profile.proteinTarget} g`],['Walk',`${state.profile.walkStart}–${state.profile.walkEnd}`]].map(x=>`<div class="kpi-card"><span class="label">${x[0]}</span><span class="value" style="font-size:1rem">${x[1]}</span></div>`).join('')}</div>`}
async function exportJson(silent=false){const payload={schemaVersion:1,app:'Manarth Daily Personal Tracker',exportedAt:new Date().toISOS
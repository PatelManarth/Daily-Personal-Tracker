tring(),...await db.exportAll()};download(`manarth-tracker-backup-${today()}.json`,JSON.stringify(payload,null,2),'application/json');state.meta.lastBackupAt=payload.exportedAt;await db.put('meta',{key:'lastBackupAt',value:payload.exportedAt});if(!silent)toast('Full backup exported.');return payload}
async function importJson(file){let p;try{p=JSON.parse(await file.text())}catch{return toast('Invalid JSON file.')}const keys=['dailyLogs','recipes','products','weeklyCheckins','measurements','adjustments','settings','meta'];if(!keys.every(k=>Array.isArray(p[k])))return toast('Backup validation failed.');if(!confirm(`Replace local data with backup from ${p.exportedAt||'unknown date'}?`))return;await exportJson(true);await db.replaceDatabase(p);await init();toast('Backup restored.');}
function csvCell(v){const s=v==null?'':String(v);return /[",\n]/.test(s)?`"${s.replaceAll('"','""')}"`:s}
function csv(rows){if(!rows.length)return '';const keys=[...new Set(rows.flatMap(Object.keys))];return [keys.join(','),...rows.map(r=>keys.map(k=>csvCell(r[k])).join(','))].join('\n')}
function exportCsv(type){let rows,name;if(type==='daily'){rows=state.logs;name='daily-logs'}else if(type==='weight'){rows=state.logs.filter(l=>Number.isFinite(l.weightKg)).map(l=>({date:l.date,weightKg:l.weightKg,validMorningWeight:validWeight(l),weighInTime:l.weighInTime,notes:l.weightNotes}));name='weight'}else if(type==='weekly'){const starts=[];for(let d=monday(state.profile.startDate);d<=state.profile.deadline;d=addDays(d,7))starts.push(d);rows=starts.map(d=>weeklySummary(d));name='weekly-summaries'}else if(type==='measurements'){rows=state.measurements;name='measurements'}else{rows=state.adjustments;name='adjustments'}download(`${name}-${today()}.csv`,csv(rows),'text/csv;charset=utf-8');}
function calendar(){const stamp=new Date().toISOString().replace(/[-:]/g,'').replace(/\.\d{3}/,''),start=state.profile.startDate.replaceAll('-',''),until=state.profile.deadline.replaceAll('-','');const events=ROUTINE_REMINDERS.map(r=>{const[h,m]=r.time.split(':');return `BEGIN:VEVENT\r\nUID:${h}${m}@manarth-tracker\r\nDTSTAMP:${stamp}\r\nDTSTART;TZID=${state.profile.timezone}:${start}T${h}${m}00\r\nRRULE:FREQ=DAILY;UNTIL=${until}T235959\r\nSUMMARY:${r.label}\r\nDURATION:PT10M\r\nEND:VEVENT`});events.push(`BEGIN:VEVENT\r\nUID:sunday@manarth-tracker\r\nDTSTAMP:${stamp}\r\nDTSTART;TZID=${state.profile.timezone}:${start}T190000\r\nRRULE:FREQ=WEEKLY;BYDAY=SU;UNTIL=${until}T235959\r\nSUMMARY:Complete weekly check-in and export backup\r\nDURATION:PT30M\r\nEND:VEVENT`);download('manarth-daily-routine.ics',`BEGIN:VCALENDAR\r\nVERSION:2.0\r\nPRODID:-//Manarth Tracker//EN\r\n${events.join('\r\n')}\r\nEND:VCALENDAR`,'text/calendar')}
async function refreshGoal(){const hits=state.logs.filter(l=>l.date<=state.profile.deadline&&validWeight(l)&&l.weightKg<=state.profile.goalWeightKg).sort((a,b)=>a.date.localeCompare(b.date));if(hits.length){const first=hits[0],lowest=Math.min(...hits.map(x=>x.weightKg));state.meta.goalAchievement={firstDate:first.date,firstWeight:first.weightKg,lowest};await db.put('meta',{key:'goalAchievement',value:state.meta.goalAchievement})}}
function banner(){const d=today(),last=state.meta.lastBackupAt?.slice(0,10);const due=parseDate(d).getDay()===0||!last||daysBetween(last,d)>=7;$('#banner').hidden=!due;if(due)$('#banner').textContent='Backup reminder: records live only in this browser. Complete the Sunday check-in and export a JSON backup.'}
function bind(){$$('#nav [data-view]').forEach(b=>b.onclick=()=>show(b.dataset.view));$$('[data-open]').forEach(b=>b.onclick=()=>show(b.dataset.open));$('#log-date').onchange=e=>loadDaily(e.target.value);$('#daily-form').onsubmit=saveDaily;$('#daily-form').oninput=updateDailyHints;$('#copy-yesterday').onclick=()=>{const y=getLog(addDays($('#log-date').value,-1));if(!y)return toast('No previous meal plan.');['breakfastCode','lunchCode','snackCode','topUpCode'].forEach(n=>$(`[name="${n}"]`).value=y[n]||'');toast('Yesterday’s meals copied.');};$('#delete-log').onclick=async()=>{const d=$('#log-date').value;if(getLog(d)&&confirm(`Delete ${d}?`)){await db.delete('dailyLogs',d);state.logs=state.logs.filter(x=>x.date!==d);loadDaily(d);renderToday();toast('Daily log deleted.')}};['plan-b','plan-l','plan-s','plan-t'].forEach(id=>$('#'+id).onchange=planUpdate);$('#meal-date').onchange=renderMeals;$('#save-plan').onclick=savePlan;$('#recipe-filter').onchange=renderRecipes;$('#recipe-search').oninput=renderRecipes;$('#week-date').onchange=renderWeekly;$('#weekly-form').onsubmit=saveWeekly;$('#measurement-form').onsubmit=saveMeasurement;$('#adjustment-form').onsubmit=saveAdjustment;$('#export-json').onclick=()=>exportJson();$('#backup-top').onclick=()=>exportJson();$('#import-button').onclick=()=>$('#import-file').click();$('#import-file').onchange=e=>{if(e.target.files[0])importJson(e.target.files[0]);e.target.value=''};$$('.csv').forEach(b=>b.onclick=()=>exportCsv(b.dataset.type));$('#calendar').onclick=calendar;$('#theme').onclick=async()=>{const dark=document.documentElement.dataset.theme==='dark';document.documentElement.dataset.theme=dark?'light':'dark';state.profile.theme=dark?'light':'dark';await db.put('settings',{key:'profile',value:state.profile})};}
async function init(){const profile=await db.get('settings','profile');if(profile?.value)state.profile=profile.value;else await db.put('settings',{key:'profile',value:state.profile});for(const [store,key] of [['dailyLogs','logs'],['recipes','recipes'],['products','products'],['weeklyCheckins','weekly'],['measurements','measurements'],['adjustments','adjustments']])state[key]=await db.getAll(store);if(!state.recipes.length){state.recipes=clone(DEFAULT_RECIPES);await db.bulkPut('recipes',state.recipes)}if(!state.products.length){state.products=clone(DEFAULT_PRODUCTS);await db.bulkPut('products',state.products)}for(const m of await db.getAll('meta'))state.meta[m.key]=m.value;document.documentElement.dataset.theme=state.profile.theme==='system'?(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'):state.profile.theme;populate();const d=today();$('#log-date').min=state.profile.startDate;$('#meal-date').min=state.profile.startDate;$('#measurement-form [name=date]').value=d;$('#adjustment-form [name=date]').value=d;$('#week-date').value=sunday(d);$('#meal-date').value=d;loadDaily(d);banner();renderToday();renderMeals();renderWeekly();renderMeasurements();renderAdjustments();renderSettings();}
bind();init().then(()=>{if('service
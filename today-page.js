import {db,DEFAULT_PROFILE,ROUTINE_REMINDERS,$,clone,esc,parseDate,daysBetween,today,fmt,validWeight,target,rolling,previousRolling,kpi,loadProfile,ensureRecipes,initializeShell} from './page-common.js';
import {renderLineChart} from './charts.js';
const state={profile:clone(DEFAULT_PROFILE),logs:[],recipes:[]};
const getLog=d=>state.logs.find(x=>x.date===d);
function status(end){const cur=rolling(state.profile,state.logs,end),prev=previousRolling(state.profile,state.logs,end);if(!Number.isFinite(cur)||!Number.isFinite(prev))return['Review required','Two complete seven-day periods with at least four valid readings each are needed.'];const gap=cur-target(state.profile,end);if(gap<-.15)return['Ahead of trajectory','The rolling average is below the required planning line.'];if(gap<=.5)return['On trajectory','The rolling average is close to the required planning line.'];if(gap<=1)return['Slightly behind','The rolling average is about 0.5–1.0 kg above the planning line.'];return['Review required','Review the complete two-week trend before changing calories, steps or exercise.']}
function chart(end){const dates=[];for(let i=27;i>=0;i--){const d=new Date(end+'T12:00:00');d.setDate(d.getDate()-i);dates.push(d.toISOString().slice(0,10))}renderLineChart($('#weight-chart'),{labels:dates.map(d=>d.slice(5)),series:[{name:'Daily weight',values:dates.map(d=>{const l=getLog(d);return validWeight(l)?l.weightKg:undefined}),line:false},{name:'7-day average',values:dates.map(d=>rolling(state.profile,state.logs,d))},{name:'Required trajectory',values:dates.map(d=>target(state.profile,d)),points:false}],annotations:[{value:state.profile.goalWeightKg,label:`Goal ${state.profile.goalWeightKg} kg`}],height:300})}
function render(){const d=today(state.profile),l=getLog(d)||{},r=rolling(state.profile,state.logs,d),prev=previousRolling(state.profile,state.logs,d),latest=state.logs.filter(validWeight).filter(x=>x.date<=d).sort((a,b)=>b.date.localeCompare(a.date))[0],distance=latest?Math.max(0,latest.weightKg-state.profile.goalWeightKg):undefined,days=Math.max(0,daysBetween(d,state.profile.deadline));const now=new Intl.DateTimeFormat('en-CA',{timeZone:state.profile.timezone,hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date()),next=ROUTINE_REMINDERS.find(x=>x.time>=now)||ROUTINE_REMINDERS[0];$('#page-root').innerHTML=`<main class="page-main"><section class="page-hero"><div><p class="eyebrow">Today</p><h2>Stay focused on the next useful action</h2><p>Your data remains in this browser. Log nutrition in Cronometer, then record the totals here.</p></div><div class="page-actions"><a class="button primary" href="daily.html">Log today</a><a class="button secondary" href="meals.html">Choose meals</a></div></section><div class="quick-action-grid"><a class="quick-action-card" href="daily.html"><span>✎</span><strong>Daily log</strong><small>Weight, nutrition, movement and recovery</small></a><a class="quick-action-card" href="meals.html"><span>🍽</span><strong>Meal library</strong><small>Choose using time, ingredients and macros</small></a><a class="quick-action-card" href="weekly.html"><span>▦</span><strong>Weekly check-in</strong><small>Review adherence and sustainability</small></a><a class="quick-action-card" href="settings.html#backup"><span>⇩</span><strong>Backup</strong><small>Export your local records</small></a></div><div class="notice info">Next routine: ${esc(next.time)} — ${esc(next.label)}</div><div class="kpi-grid">${[kpi('Morning weight',Number.isFinite(l.weightKg)?`${l.weightKg.toFixed(1)} kg`:'No data',validWeight(l)?'Valid morning reading':'Needs normal conditions'),kpi('7-day average',Number.isFinite(r)?`${r.toFixed(1)} kg`:'No data',Number.isFinite(r)&&Number.isFinite(prev)?`${(prev-r).toFixed(2)} kg lost vs prior period`:'At least 4 valid readings'),kpi('Distance to goal',Number.isFinite(distance)?`${distance.toFixed(1)} kg`:'No data'),kpi('Days remaining',String(days),`Deadline ${fmt(state.profile.deadline)}`),kpi('Calories',Number.isFinite(l.calories)?String(l.calories):'No data','Target 1,500–1,600'),kpi('Protein',Number.isFinite(l.proteinG)?`${l.proteinG} g`:'No data','Minimum 110 g'),kpi('Morning walk',l.walkStatus||'Pending',`${l.walkMinutes||0}/60 min • ${l.steps||0} steps`),kpi('Recovery',Number.isFinite(l.sleepHours)?`${l.sleepHours} h sleep`:'No data',`${l.waterL||0} L water`)].join('')}</div><div class="two-column"><article class="card"><div class="card-heading"><h3>Today's meals</h3><a class="text-button" href="meals.html">Change</a></div>${[['Breakfast',l.breakfastCode],['Lunch',l.lunchCode],['Snack',l.snackCode],['Top-up',l.topUpCode]].map(([n,c])=>{const r=state.recipes.find(x=>x.code===c);return `<div class="meal-row"><strong>${n}</strong><span>${r?`${esc(r.code)} — ${esc(r.name)}`:'Not selected'}</span></div>`}).join('')}</article><article class="card"><h3>Today's priorities</h3>${[['Morning walk',l.walkStatus==='Completed'?'Completed':'Pending'],['Cronometer totals',Number.isFinite(l.calories)&&Number.isFinite(l.proteinG)?'Completed':'Pending'],['Daily log',l.completed?'Completed':'Pending'],['Sunday backup',parseDate(d).getDay()===0?'Due today':'Weekly']].map(([a,b])=>`<div class="action-row"><strong>${a}</strong><span class="status-chip ${b==='Completed'?'success':'warning'}">${b}</span></div>`).join('')}</article></div><article class="card"><h3>Weight trend</h3><div id="weight-chart" class="chart-container"></div><p id="trend-message" class="notice neutral"></p></article></main>`;chart(d);const s=status(d);$('#trend-message').innerHTML=`<strong>${s[0]}.</strong> ${s[1]}`}
async function init(){state.profile=await loadProfile();state.logs=await db.getAll('dailyLogs');state.recipes=await ensureRecipes();initializeShell(state.profile,'today','Today','Your plan, progress and next actions in one place.');render()}
init().catch(console.error);